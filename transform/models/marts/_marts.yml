# GENERATED by engine/render.py from stack.yaml. Do not edit; edit stack.yaml.
version: 2

models:
  - name: mart_revenue_by_category
    description: "Revenue and margin-giveaway per product category."
    columns:
      - name: product_category
        description: "Grain: product_category"
        data_tests: [not_null]
      - name: revenue
        description: "Net revenue after discount (SUM(net_revenue))"
      - name: gross_revenue
        description: "Revenue before discount (SUM(gross_revenue))"
      - name: discount_amount
        description: "Total discount given (SUM(discount_amount))"
      - name: orders
        description: "Number of orders (COUNT(DISTINCT order_id))"
      - name: units
        description: "Units sold (SUM(quantity))"
      - name: avg_order_value
        description: "Net revenue per order (ROUND(SUM(net_revenue) / NULLIF(COUNT(DISTINCT order_id), 0), 2))"
      - name: avg_discount_pct
        description: "Average discount, in percent (ROUND(AVG(discount) * 100, 2))"
      - name: avg_rating
        description: "Average customer rating (ROUND(AVG(customer_rating), 2))"
  - name: mart_revenue_by_region
    description: "Revenue and service quality per region."
    columns:
      - name: region
        description: "Grain: region"
        data_tests: [not_null]
      - name: revenue
        description: "Net revenue after discount (SUM(net_revenue))"
      - name: orders
        description: "Number of orders (COUNT(DISTINCT order_id))"
      - name: units
        description: "Units sold (SUM(quantity))"
      - name: avg_order_value
        description: "Net revenue per order (ROUND(SUM(net_revenue) / NULLIF(COUNT(DISTINCT order_id), 0), 2))"
      - name: avg_discount_pct
        description: "Average discount, in percent (ROUND(AVG(discount) * 100, 2))"
      - name: avg_rating
        description: "Average customer rating (ROUND(AVG(customer_rating), 2))"
      - name: avg_delivery_days
        description: "Average days to delivery (ROUND(AVG(delivery_days), 1))"
  - name: mart_revenue_monthly
    description: "Monthly revenue trend across the whole dataset."
    columns:
      - name: order_month
        description: "Grain: order_month"
        data_tests: [not_null]
      - name: revenue
        description: "Net revenue after discount (SUM(net_revenue))"
      - name: gross_revenue
        description: "Revenue before discount (SUM(gross_revenue))"
      - name: discount_amount
        description: "Total discount given (SUM(discount_amount))"
      - name: orders
        description: "Number of orders (COUNT(DISTINCT order_id))"
      - name: units
        description: "Units sold (SUM(quantity))"
      - name: avg_order_value
        description: "Net revenue per order (ROUND(SUM(net_revenue) / NULLIF(COUNT(DISTINCT order_id), 0), 2))"
  - name: mart_category_region
    description: "Category by region cross-tab — where each category actually sells."
    columns:
      - name: product_category
        description: "Grain: product_category"
        data_tests: [not_null]
      - name: region
        description: "Grain: region"
      - name: revenue
        description: "Net revenue after discount (SUM(net_revenue))"
      - name: orders
        description: "Number of orders (COUNT(DISTINCT order_id))"
      - name: units
        description: "Units sold (SUM(quantity))"
      - name: avg_order_value
        description: "Net revenue per order (ROUND(SUM(net_revenue) / NULLIF(COUNT(DISTINCT order_id), 0), 2))"
      - name: avg_rating
        description: "Average customer rating (ROUND(AVG(customer_rating), 2))"
  - name: mart_payment_method
    description: "Revenue and delivery experience per payment method."
    columns:
      - name: payment_method
        description: "Grain: payment_method"
        data_tests: [not_null]
      - name: revenue
        description: "Net revenue after discount (SUM(net_revenue))"
      - name: orders
        description: "Number of orders (COUNT(DISTINCT order_id))"
      - name: avg_order_value
        description: "Net revenue per order (ROUND(SUM(net_revenue) / NULLIF(COUNT(DISTINCT order_id), 0), 2))"
      - name: avg_discount_pct
        description: "Average discount, in percent (ROUND(AVG(discount) * 100, 2))"
      - name: avg_rating
        description: "Average customer rating (ROUND(AVG(customer_rating), 2))"
      - name: avg_delivery_days
        description: "Average days to delivery (ROUND(AVG(delivery_days), 1))"
  - name: mart_customer_value
    description: "Per-customer lifetime value and activity window."
    columns:
      - name: customer_id
        description: "Grain: customer_id"
        data_tests: [not_null]
      - name: revenue
        description: "Net revenue after discount (SUM(net_revenue))"
      - name: orders
        description: "Number of orders (COUNT(DISTINCT order_id))"
      - name: units
        description: "Units sold (SUM(quantity))"
      - name: avg_order_value
        description: "Net revenue per order (ROUND(SUM(net_revenue) / NULLIF(COUNT(DISTINCT order_id), 0), 2))"
      - name: avg_rating
        description: "Average customer rating (ROUND(AVG(customer_rating), 2))"
      - name: first_order_date
        description: "First order (MIN(order_date))"
      - name: last_order_date
        description: "Most recent order (MAX(order_date))"
      - name: active_days
        description: "Days between first and last order (MAX(order_date) - MIN(order_date))"
