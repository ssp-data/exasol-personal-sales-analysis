[project]
name = "sales-analytics"
version = "0.1.0"
description = "A minimal declarative data stack on Exasol Personal: CSV -> data marts -> natural language over MCP."
requires-python = ">=3.11,<3.13"
dependencies = [
    "dbt-core>=1.12,<1.13",
    "dbt-exasol>=1.12,<1.13",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
