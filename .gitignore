# Build artefacts and the normalised copy of the source file.
build/

# Tooling
.venv/
transform/target/
transform/logs/
transform/dbt_packages/
__pycache__/
*.pyc
.user.yml
.mypy_cache/
.ruff_cache/
.claude

# Note: transform/models/marts/** IS committed even though the engine generates
# it. It is the "declarative code" artefact — reviewable in a pull request,
# diffable when stack.yaml changes. Never edit it by hand; `make render`
# overwrites it.
